export class Origin {
	constructor(codeBlock, domain, publicCodeBlock, vendor) {
		this.codeBlock = codeBlock;
		this.domain = domain;
		this.publicCodeBlock = publicCodeBlock;
		this.vendor = vendor;
	}
}